<!DOCTYPE html>
<html lang="en">
<%- include('./components/head.ejs') %>
<body>
    


<%- include('./components/sidebar.ejs') %>
<%- include('./components/topnav.ejs') %>

<div class="content ml-64">
<div style="margin-bottom: 50px;"></div>
    <% if (jsonData && jsonData['eggs-json']) { %>
        <% Object.keys(jsonData['eggs-json']).forEach(egg => { %>
            <% if (jsonData['eggs-json'][egg].content && jsonData['eggs-json'][egg].content.name) { %>
                <li><%= jsonData['eggs-json'][egg].content.name %></li>
            <% } %>
        <% }); %>
    <% } else { %>
        <li>No eggs exists</li>
    <% } %>
</div>
</body>
</html>
